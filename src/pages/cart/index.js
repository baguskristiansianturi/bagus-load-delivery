import {store,money} from '../../app/store.js';import {shell,icon,toast} from '../../app/ui.js';
export function cart(){const a=store.getCart(),t=store.cartTotal();return shell(a.length?`<div class="section-heading compact"><span class="section-kicker">PESANAN</span><h1>Keranjang</h1></div><div class="cart-layout"><div class="cart-list">${a.map(x=>`<article class="cart-item"><img src="${x.image}"><div><b>${x.title}</b><span>${x.provider}</span><strong>Rp ${money(x.price)}</strong></div><div class="cart-qty"><button data-m="${x.id}">−</button><b>${x.quantity}</b><button data-p="${x.id}">+</button></div><button data-r="${x.id}">Hapus</button></article>`).join('')}</div><aside class="summary-card"><h3>Ringkasan</h3><div><span>Subtotal</span><b>Rp ${money(t)}</b></div><hr><div class="summary-total"><span>Total sementara</span><strong>Rp ${money(t)}</strong></div><a class="primary-button wide-button" href="#/checkout">Lanjut checkout ${icon.arrow}</a><p>Pengiriman dikonfirmasi setelah lokasi proyek dipilih.</p></aside></div>`:`<div class="empty-state large"><h1>Keranjang kosong</h1><p>Pilih material, jasa atau logistik.</p><a class="primary-button" href="#/material">Mulai mencari</a></div>`,'Keranjang')||'';queueMicrotask(()=>{document.querySelectorAll('[data-p]').forEach(b=>b.onclick=()=>{const x=store.getCart().find(x=>x.id===b.dataset.p);store.updateCart(x.id,x.quantity+1);location.hash='#/cart'});document.querySelectorAll('[data-m]').forEach(b=>b.onclick=()=>{const x=store.getCart().find(x=>x.id===b.dataset.m);store.updateCart(x.id,x.quantity-1);location.hash='#/cart'});document.querySelectorAll('[data-r]').forEach(b=>b.onclick=()=>{store.removeFromCart(b.dataset.r);toast('Item dihapus');location.hash='#/cart'})});return html}

import {store} from '../../app/store.js';import {shell,hero,card,bind} from '../../app/ui.js';export function wishlist(){const a=store.getWishlist();const h=shell(`${hero('TERSIMPAN','Wishlist','Simpan pilihan yang ingin dibandingkan atau dipesan nanti.')}${a.length?`<div class="catalog-grid">${a.map(card).join('')}</div>`:'<div class="empty-state"><h2>Belum ada yang disimpan</h2><p>Tekan ikon hati pada item.</p></div>'}`,'Wishlist');queueMicrotask(bind);return h}

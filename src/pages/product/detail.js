import {getItem} from '../../data/catalog.js';import {store,money} from '../../app/store.js';import {shell,icon,bind,toast} from '../../app/ui.js';
export function detail(id){const x=getItem(id);if(!x)return shell('<div class="empty-state"><h2>Item tidak ditemukan</h2><a class="primary-button" href="#/">Beranda</a></div>','Tidak ditemukan');const html=shell(`<div class="detail-layout"><div class="detail-gallery"><img src="${x.image}" alt="${x.title}"></div><section class="detail-info"><small>${x.type.toUpperCase()}</small><h1>${x.title}</h1><div>★ ${x.rating} · ${x.provider}</div><div class="detail-price">Rp ${money(x.price)} <small>/ ${x.unit}</small></div><p class="detail-status">● ${x.stock}</p><div class="detail-provider"><b>${x.provider}</b><span>Area Bali · informasi penyedia ditampilkan sejak awal</span></div><h3>Catatan</h3><ul><li>Harga dapat menyesuaikan jumlah dan lokasi.</li><li>Ketersediaan dikonfirmasi sebelum pembayaran.</li><li>Detail pengiriman dibicarakan setelah pesanan dibuat.</li></ul><div class="detail-actions"><button class="secondary-button" id="wish">${store.isWishlisted(x.id)?'♥ Tersimpan':'♡ Simpan'}</button>${x.type==='property'?`<a class="primary-button" href="#/checkout?item=${x.id}">Ajukan kebutuhan</a>`:`<button class="primary-button" id="add">${icon.cart} Tambah ke keranjang</button>`}</div></section></div><div class="detail-trust"><b>Harga jelas</b><b>Penyedia terlihat</b><b>Alur pesanan sederhana</b></div>` ,x.title);queueMicrotask(()=>{bind();document.querySelector('#add')?.addEventListener('click',()=>{store.addToCart(x);toast('Ditambahkan ke keranjang');setTimeout(()=>location.hash='#/cart',350)});document.querySelector('#wish')?.addEventListener('click',e=>{store.toggleWishlist(x);e.currentTarget.textContent=store.isWishlisted(x.id)?'♥ Tersimpan':'♡ Simpan'})});return html}
